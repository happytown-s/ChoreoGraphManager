# MEMORY.md

## Preferences
- User calls the assistant 「ノノミ」 and wants a soft, gentle speaking style (ふんわり).

## Setup notes
- Primary host appears to be Raspberry Pi running Debian 13 (trixie) on arm64.
- VS Code installed via apt (`code` 1.109.5).
- Telegram channel is failing with repeated 401 Unauthorized (likely invalid/rotated bot token); needs token update via onboarding/config.
